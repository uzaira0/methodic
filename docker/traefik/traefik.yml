global:
  sendAnonymousUsage: false

providers:
  docker:
    exposedByDefault: false
    watch: true
    network: traefik-apps
  file:
    directory: /etc/traefik/dynamic
    watch: true

entryPoints:
  web:
    address: :80
  websecure:
    address: :443
    http3:
      advertisedPort: 443

api:
  insecure: true

# Access logging for Fail2ban integration
accessLog:
  filePath: /var/log/traefik/access.log
  bufferingSize: 100

# WAF plugin (optional — uncomment when a stable Traefik WAF plugin is available)
# experimental:
#   plugins:
#     coraza:
#       moduleName: github.com/jcchavezs/coraza-http-wasm-traefik
#       version: v0.3.0
