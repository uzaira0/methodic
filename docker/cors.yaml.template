# CORS Configuration
# Rendered by envsubst at container startup
# Note: flat structure, kebab-case keys (Jackson + Rhizome ConfigurationLoader convention)
allowed-origins:
  - "https://${DOMAIN}"
  - "http://${DOMAIN}"
  # EXT_DOMAIN is optional — backend CORS filter should ignore empty strings
  - "https://${EXT_DOMAIN}"
