enabled: ${MOBILE_SIGNING_ENABLED:-false}
signing-secret: "${MOBILE_SIGNING_SECRET}"
signing-required: ${MOBILE_SIGNING_REQUIRED:-false}
max-request-age-minutes: 5
clock-skew-seconds: 30
nonce-ttl-minutes: 10
