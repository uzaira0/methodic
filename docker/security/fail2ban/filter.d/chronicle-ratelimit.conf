# Fail2ban filter for Chronicle rate-limited responses (HTTP 429)
# Matches Traefik common log format access logs

[Definition]
failregex = ^<HOST> - .* "(?:GET|POST|PUT|DELETE|PATCH) .* HTTP/[12]\.[01]" 429 .*$
ignoreregex =
