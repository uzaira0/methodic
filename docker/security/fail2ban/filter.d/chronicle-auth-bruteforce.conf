# Fail2ban filter for Chronicle auth brute force (HTTP 401)
# Matches Traefik common log format access logs

[Definition]
failregex = ^<HOST> - .* "(?:GET|POST|PUT|DELETE|PATCH) .* HTTP/[12]\.[01]" 401 .*$
ignoreregex =
