# Fail2ban filter for path scanning detection (HTTP 404)
# Matches Traefik common log format access logs

[Definition]
failregex = ^<HOST> - .* "(?:GET|POST|PUT|DELETE|PATCH) .* HTTP/[12]\.[01]" 404 .*$
ignoreregex =
