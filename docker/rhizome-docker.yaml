enable-persistence: false
session-clustering-enabled: false

hazelcast:
  server: true
  group: "local"
  password: "trellis"
  cp-member-count: 0
  seed-nodes:
    - "localhost"
  port: 5710

hazelcast-clients:
  IDS:
    server: false
    group: "local"
    password: "test"
    cp-member-count: 0
    seed-nodes:
      - "127.0.0.1:5710"

postgres:
  citus: false
  hikari:
    # SSL/TLS encryption in transit configuration
    # sslmode options: disable, allow, prefer, require, verify-ca, verify-full
    # For HIPAA compliance, use 'require' (minimum) or 'verify-full' (recommended)
    # sslrootcert path should match the mounted CA certificate in docker-compose
    jdbcUrl: "jdbc:postgresql://postgres:5432/chronicle?sslmode=require&sslrootcert=/app/ssl/ca.crt"
    username: "chronicle"
    password: "6vFWSRerk6cocgcGl9d5vnUBTu2syUgcy2KFmMyU10g="
    maximumPoolSize: 20

datasources:
  chronicle:
    citus: false
    initialize-tables: true
    hikari:
      # SSL/TLS encryption in transit configuration
      jdbcUrl: "jdbc:postgresql://postgres:5432/chronicle?sslmode=require&sslrootcert=/app/ssl/ca.crt"
      username: "chronicle"
      password: "6vFWSRerk6cocgcGl9d5vnUBTu2syUgcy2KFmMyU10g="
      maximumPoolSize: 20
