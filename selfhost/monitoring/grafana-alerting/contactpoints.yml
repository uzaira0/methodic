{
  "apiVersion": 1,
  "contactPoints": [
    {
      "orgId": 1,
      "name": "chronicle-dashboard-only",
      "receivers": [
        {
          "uid": "chronicle-dashboard-only",
          "type": "webhook",
          "settings": {
            "url": "$CHRONICLE_ALERT_WEBHOOK_URL",
            "httpMethod": "POST",
            "title": "Chronicle self-host alert"
          },
          "disableResolveMessage": true
        }
      ]
    }
  ]
}
