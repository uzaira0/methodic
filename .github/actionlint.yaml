# actionlint config — see https://github.com/rhysd/actionlint/blob/main/docs/config.md
self-hosted-runner:
  labels: []
# config-variables omitted intentionally → all `vars.*` references allowed.
# Add explicit list here to enforce typo-checking on configuration variables.
